// vim: set tabstop=2 softtabstop=2 shiftwidth=2 noexpandtab textwidth=80 :

package com.example.htmlapp.model.enums;

/**
 * Enumeración que representa la dirección de ordenación.
 *
 * Usar un enum evita depender de comparaciones de cadenas como
 * "ASC" o "DESC", proporcionando un conjunto fijo de valores válidos.
 */
public enum SortDirection {
	ASC,
	DESC;
}